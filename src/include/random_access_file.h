#pragma once

namespace emerald {
class RandomAccessFile {
/**
 * This is a dummy class, that has to be designed to replicate the
 * functionality of the Java API RandomAccessFile class.
 * 
 * This is used in the LogFile class, and should be removed if the
 * desired functionality can be replicated using streams.
 */
};
}
